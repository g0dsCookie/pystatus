<?xml version="1.0"?>
<pystatus>
    <blocks>
        <block plugin="uptime"/>

        <block plugin="battery"/>

        <block plugin="cpu"/>

        <!-- Temperature
        <block plugin="temperature" name="cpu">
            <text>C: {:.0f}°C</text>
            <path>temp_path</path>
        </block>
        -->

        <block plugin="loadavg"/>

        <block plugin="disk" name="/"/>

        <!-- ZFS
        <block plugin="zfs" name="dataset"/>
        <-->

        <block plugin="memory"/>

        <!-- SWAP
        <block plugin="memory" name="swap">
            <text>S: {}</text>
            <type>swap</type>
        </block>
        -->

        <block plugin="wifi" name="wlan0"/>

        <block plugin="clock"/>
    </blocks>
</pystatus>