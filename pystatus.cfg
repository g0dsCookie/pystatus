<?xml version="1.0"?>
<pystatus>
    <blocks>
        <block plugin="uptime"/>
        <block plugin="cpu"/>
        <block plugin="loadavg"/>

        <!-- ZFS
        <block plugin="zfs">
            <dataset>ssd</dataset>
        </block>
        -->

        <block plugin="memory"/>

        <!-- SWAP
        <block plugin="memory" name="swap">
            <text>S: {}</text>
            <type>swap</type>
        </block>
        -->

        <block plugin="clock"/>
    </blocks>
</pystatus>